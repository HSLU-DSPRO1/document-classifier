# Input data

Place the raw datasets in this folder.

Expected filenames:

- `enron.csv`
- `invoices.csv`
- `invoices_2.csv`
- `arxiv.jsonl` (preferred) or `arxiv.json`

Optional (for demo):

- `pdfs/` → put `.pdf` files here for `04_pdf_inference.ipynb`

Notes:
- Do not commit large/raw datasets to the repository.
- Keep only a small `input/pdfs/` demo set if licensing allows.
